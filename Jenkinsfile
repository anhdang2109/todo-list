pipeline {
    agent {  }
    stages {
        stage('docker-agent') {
            steps('Clone source code') {
                git 'https://github.com/anhdang2109/todo-list.git'
            }
        }
    }
}
